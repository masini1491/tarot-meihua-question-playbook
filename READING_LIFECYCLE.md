# 占卜生命週期（Reading Lifecycle）

本章是跨塔羅與梅花易數共同的**占問生命週期 authority**，負責判斷：這是不是新題、何時能承接、何時能補占／重占、現實新資訊出現後怎麼轉題、什麼情況算完成，以及舊占如何回測。

本章不定義牌義、卦義、牌陣或起卦算法；方法差異分別由 `TAROT.md`、`MEIHUA.md` 維護。ChatGPT 最終怎麼呈現答案、怎麼交付可複製題目，仍由 `CHATGPT_OUTPUT.md` 維護。

核心原則：**一次占問不是孤立的一段文字，而是一個有前提、結果、後續節點、現實驗證與關閉條件的判斷單位。**

## 1. 基本生命週期

建議把一次占問理解為：

```text
QUESTION DRAFT
  ↓
CONTRACT FIXED
  ↓
DRAW / CAST
  ↓
INTERPRETED
  ↓
WAITING FOR REALITY
  ├─ 新現實資訊 → NEW JUDGMENT NODE / FOLLOW-UP
  ├─ 明確 unresolved function → FOLLOW-UP
  ├─ completion rule 達成 → RESOLVED
  └─ horizon 結束／現實結果可判定 → RESOLVED
        ↓
      BACKTEST（需要時）
```

不是每一題都要走完整流程；狀態題、原因題或純反思題可能沒有 `WAITING FOR REALITY`。但只要涉及預測、時間或條件分支，就應保留前提與完成條件，避免後面失去題目身份。

## 2. Evidence Layers｜占問紀錄分層

若需要保存或回測，一次 reading 應至少把以下層級分開，不讓後來內容覆蓋前面內容：

```text
1. QUESTION / CONTRACT FACT
2. DRAW / CAST FACT
3. ORIGINAL INTERPRETATION
4. REALITY UPDATE
5. RETROSPECTIVE INTERPRETATION
6. BACKTEST JUDGMENT
```

### 2.1 QUESTION / CONTRACT FACT

保存抽牌／起卦前已固定的：

- 題目；
- `subject`；
- `horizon`；
- `completion_rule`；
- 牌位／起卦規則；
- `exclusions`；
- 當時已確認的 `context_facts`。

後續不得因結果或現實發展而回寫成另一個版本；若契約真的有缺陷，依修正版規則建立新版本並保留原版。

### 2.2 DRAW / CAST FACT

保存當次實際牌面、正逆位、A/B、卦象、動爻與必要 provenance。

這是工具／實體抽取的事實層，不包含任何解讀。

### 2.3 ORIGINAL INTERPRETATION

保存當時在不知道未來結果的前提下實際寫出的主結論、次級分支與限制。

若後來重新解讀，不覆蓋這一層。

### 2.4 REALITY UPDATE

只保存後來**現實中真正發生或可驗證**的資訊，例如正式通知、實際見面、物流狀態、合作成立、期限結束等。

Reality Update 不等於新的 interpretation；也不得把前占象徵結論寫進這一層。

### 2.5 RETROSPECTIVE INTERPRETATION

知道後續現實後重新看到的牌／卦語意，必須獨立保存並標記為 retrospective。

它可以幫助方法研究，但不能倒灌成「當時其實已預測到」。

### 2.6 BACKTEST JUDGMENT

最後才依原契約、原始結果、原始解讀與現實結果做回測判定。

核心原則：

> **Interpretation is not reality evidence; retrospective insight is not original prediction。**

若紀錄系統支援修改，優先採 append-only：新增 interpretation、reality update、reflection 或 backtest，而不是覆蓋舊紀錄。修正拼字或明確資料錯誤時，也應盡量保留變更痕跡。

## 3. Question Identity Gate｜先判斷是不是新的判斷單位

符合下列任一情況，通常可以視為**新的判斷節點（new judgment node）**：

- 已確認的現實事實改變原題前提；
- 原題的大分支已被明確選定，現在只問該分支內的新功能；
- 主要判斷對象、人物或事件已改變；
- `horizon`、`completion_rule` 或事件層級被明確重新定義；
- 原題存在可指出的牌位／起卦契約缺陷，需要建立修正版；
- 原題只回答是否／哪個分支，現在另問原因、時間、品質、後果等先前沒有承擔的功能。

若只是：

- 換句話；
- 把同一時間窗切成近義說法；
- 因結果不喜歡而重新問；
- 沒有新事實、沒有新功能、沒有新事件層級；

則仍視為同一題，不應因此取得重新抽牌／起卦的理由。

`INPUT_CONTRACT.md` 負責保存題目的身份欄位；本章負責判斷這些變化是否真的構成新節點。

## 4. 承接前占分成兩種，不得混成同一種事實

### A. 現實承接（real-world continuation）

前提已由現實確認，例如：

- 已收到正式通知；
- 已發生見面／合作／交易；
- 物流已正式顯示完成；
- 使用者已實際觀察到某事件。

這時新題可以直接把它寫成 `CONFIRMED FACT`，並停止重新判斷「它有沒有發生」。

### B. 象徵／條件分支承接（symbolic or conditional continuation）

前一組牌／卦只是在象徵上支持某一分支，但現實尚未確認。

這時可以承接，但應明確寫成：

> 「承接前一組占卜已顯示 X 為較強分支；**僅在 X 成立的條件世界下**，本題不重新比較 X，而只檢視……」

前占結論在這裡是**working branch / conditional premise**，不是現實事實。後續聊天室不得因為它被多次引用，就把它改寫成「既然 X 已經會發生」。

核心原則：**承接可以凍結一個判斷功能，但不能把象徵結論升格成現實證據。**

## 5. 條件世界（Conditional World）

若後續問題只有在某前提成立時才有意義，而該前提尚未由現實確認，應使用條件世界。

適合：

- 「只在我確實取得該工作機會的條件世界下，檢視……」
- 「只在雙方確實自願發生該事件的條件世界下，檢視……」
- 「只在前一題最強分支最終成立的情況下，比較……」

條件世界的作用是**隔離問題功能**，不是預先宣告前提會成真。

不應把條件世界拿來規避主分支判斷：若後續決策真正需要知道前提是否成立，仍應先有乾淨的主分支題或等待現實確認。

## 6. Follow-up Gate｜什麼時候可以補占

只有存在可命名的新功能或新判斷節點時，才應補占。常見合法原因：

- 原題留下**明確 unresolved function**；
- 原題只定位大時間窗，現在需要拆分已勝出的時間窗；
- 原題只判斷是否／哪個分支，現在問該分支內的原因、品質、時間或後果；
- 新現實資訊改變前提；
- 原題契約有具體缺陷，已建立修正版；
- 使用者明確要求對另一個獨立人物／事件建立新的平行題。

不合法原因：

- 不喜歡第一次結果；
- 想讓另一套方法再投一次票；
- 每得到一個答案就自動追問更細日期／數字；
- 沒有新功能，只是想看看會不會抽到更清楚的牌／卦。

ChatGPT 是否應主動提出下一題，另受 `CHATGPT_OUTPUT.md` 的補占輸出關卡限制。

## 7. 重抽／重卦與修正版

若原題存在契約缺陷，允許建立修正版，但必須保留：

- 原題；
- 缺陷是什麼；
- 新版修正了哪個欄位／牌位／事件層級；
- 哪一版應作為後續主要解讀依據。

修正版不能把原結果抹掉，也不能假裝第一次從未發生。

各方法的額外規則：

- 塔羅已有牌面時如何處理 → `TAROT.md`
- 梅花「一事一占」、canonical A／B 與不得切換起卦法 → `MEIHUA.md`

## 8. 多人物／多對象平行題

若同一模板分別詢問不同人物、公司、選項或事件，每一個都是**獨立 question identity**：

- 不因題幹 95% 相同就共用同一組牌／同一卦；
- 每個人物／對象各自抽牌或起卦；
- 比較時先完成各自解讀，再視需要做跨對象比較；
- ChatGPT 的題目交付必須依 `CHATGPT_OUTPUT.md` 將每個已知人物／對象完整代入並各自提供獨立 copy surface。

這裡的「同模板」只是設計重用，不代表占問結果可以共用。

## 9. 現實新資訊出現後，優先轉題而不是維持舊抽象問題

現實事實是最高優先級的狀態更新。

例如：

```text
原題：今年會不會出現工作機會？
現實：已收到某公司的面試邀請。
```

後續不應繼續重問「會不會有工作機會」，而應改成具體事件題，例如：

- 這個職缺是否值得投入；
- 面試流程最可能如何發展；
- 哪個條件是主要阻礙／助力。

同理，若某件事已經發生，新的題目可以只分析：

- 此次事件主要由哪些因素支撐；
- 發生後的品質／體驗／後果；
- 接下來的流程。

但不得重新把已確認事件降回「會不會發生」的預測題。

## 10. Completion Gate｜什麼時候一個預測算完成

若原題有 `completion_rule`，只有現實事件真正符合該規則時，該預測才進入 `RESOLVED`。

必須區分：

- 前置信號；
- 啟動；
- 進行中；
- 接近完成；
- `completion_rule` 真正達成。

例如完成規則是「收到正式可取件通知」，那麼物流移動、推測已進店、狀態顯示配送中都不能替代正式通知。

完成證據的最終輸出語氣另受 `CHATGPT_OUTPUT.md` 的 Completion Evidence Guard 約束。

## 11. Horizon 結束但事件未發生，也是一種可判定結果

若預測題明確定義 `horizon`，而期限結束時 `completion_rule` 未達成，應記錄為：

- 在該 horizon 內未完成；
- 若要繼續問未來，建立新的時間範圍與新題；
- 不把原 horizon 無限向後滑動來維持舊結論。

若現實資料不足以確認是否發生，標記 `UNRESOLVED`，不要自行補成命中或失準。

## 12. Backtest Gate｜舊占回測

舊占回測至少分開以下層級：

1. **原題／契約品質**：題型、牌位、事件層級、`completion_rule` 是否乾淨。
2. **原始牌／卦的支持程度**：依原契約重讀，不能因知道答案而換功能。
3. **當時實際主結論**：當時真正排第一或明確宣告的方向。
4. **事前次級／條件式預測**：當時確實寫過的次順位、條件分支或細節。
5. **事後重讀**：知道答案後才發現的可能語意，必須獨立標示。
6. **實際結果**：依現實與原 `completion_rule` 判定。

回測不得：

- 把事後重讀冒充當時預測；
- 因正確窗口的牌「其實也解得通」就忽略事前主排序錯誤；
- 用被污染的牌位任選其中一個功能來證明命中；
- 因單一成功／失敗案例就宣稱客觀預測效度。

可使用較細緻的結果描述，例如：

- 主結論符合；
- 主結論偏移、次級訊號符合；
- 事件層級抓對但時間偏移；
- 題目／牌位污染，無法乾淨評分；
- 現實證據不足，`UNRESOLVED`。

## 13. Historical Context 不會因保存或重複引用而自動升權

舊占、舊排序、舊聊天室結論與已失效時間窗都屬 Historical Context，除非當前題明確承接／比較／回測它們，否則不預設載入。

**Persistence does not increase prediction authority。** 一個舊占被保存、引用很多次或存在很久，不會因此變成已確認事實，也不會自動壓過新的現實資訊或新的獨立題。

Context admission 的最小啟動規則見 `CHAT_INIT.md`。

## 14. 建議的回測紀錄

需要正式回測時，可保存：

```text
原題／完成判定：
題目或牌位污染：
牌面／卦象本身支持：
當時主結論：
事前次級或條件式預測：
事後重讀：
實際結果：
結構命中：
事件層級偏移：
時間偏移：
解讀偏移：
可泛化規則：
```

這是內容欄位，不要求 ChatGPT 每次固定全部輸出；最終呈現仍以 `CHATGPT_OUTPUT.md` 為 authority。